# PROCTOR System Optimization Guide\n\n## Performance Tuning Checklist\n\n### 1. Detection Thresholds (Easy)\n\n**Location**: `App.jsx` lines 7-10\n\n```javascript\nconst GAZE_THRESHOLD = 8;       // Increase = less sensitive\nconst HEAD_THRESHOLD = 8;\nconst NOFACE_THRESHOLD = 12;\n```\n\n**When to adjust**:\n- High false positives? → Increase threshold\n- Missing real issues? → Decrease threshold\n- Recommended range: 5-15\n\n### 2. Frame Skipping (Important)\n\n**Location**: `App.jsx` lines 12-13\n\n```javascript\nconst FRAME_SKIP_FACE_DETECTION = 3;  // Run every 3 frames (30fps → 10fps)\nconst FRAME_SKIP_OBJECT_DETECTION = 5; // Run every 5 frames\n```\n\n**Performance vs Accuracy Trade-off**:\n| FPS | Latency | CPU | Accuracy |\n|-----|---------|-----|----------|\n| 30  | Smooth  | 100%| Best     |\n| 15  | Good    | 60% | Very Good|\n| 10  | Decent  | 40% | Good     |\n| 5   | Laggy   | 20% | Poor     |\n\n**Recommended**:\n- Fast machine (i7+): FRAME_SKIP = 3\n- Medium machine (i5): FRAME_SKIP = 4\n- Slow machine (i3): FRAME_SKIP = 5-6\n\n### 3. Confidence Thresholds (Precision)\n\n**Location**: `App.jsx` lines 170-171\n\n```javascript\nminDetectionConfidence: 0.4,   // 40% confidence minimum\nminTrackingConfidence: 0.55,   // 55% for tracking\n```\n\n**Trade-offs**:\n- **Lower (0.3)**: Detects more but more false positives\n- **Higher (0.6)**: Only confident detections, might miss edge cases\n- **Optimal**: 0.4-0.5 for detection, 0.5-0.6 for tracking\n\n### 4. Memory Optimization\n\n**Alert History Limit** (App.jsx, line 54):\n```javascript\nalertHistoryRef.current = [entry, ...alertHistoryRef.current].slice(0, 200);\n```\n\n**Recommended limits**:\n- Small deployments: 100-150\n- Standard: 200-300\n- Large scale: 500+\n\n### 5. Object Detection Sensitivity\n\n**COCO-SSD Confidence** (App.jsx, line 169):\n```javascript\nconst susp = preds.filter(p => SUSPICIOUS_CLASSES.has(p.class) && p.score >= 0.50);\n```\n\n**Recommended values**:\n- Strict detection: 0.6-0.7 (fewer false alarms)\n- Standard: 0.5 (balanced)\n- Lenient: 0.3-0.4 (catch everything)\n\n## CPU Usage Profiling\n\n### Monitor Performance\n\nOpen browser DevTools (F12) → Performance tab:\n\n1. **Start recording**\n2. Run proctoring for 30 seconds\n3. **Stop recording**\n4. Check FPS graph and CPU timeline\n\n**Healthy baseline**:\n- FPS: 25-30 consistently\n- GPU usage: <50%\n- Memory: <300MB\n\n### Quick Benchmark\n\n```javascript\n// Add to App.jsx\nimport { FPSCounter, PerformanceMonitor } from './advanced-utils.js';\n\nconst fpsCounter = new FPSCounter();\nconst perfMonitor = new PerformanceMonitor('Main Loop');\n\n// In runLoop():\nperfMonitor.start();\n// ... detection code ...\nperfMonitor.end();\nfpsCounter.tick();\n\n// Every 5 seconds:\nsetInterval(() => {\n  console.log(`FPS: ${fpsCounter.getFPS()}`);\n  perfMonitor.report();\n}, 5000);\n```\n\n## Optimization Techniques\n\n### 1. **Reduce Video Resolution**\n\n```javascript\n// In startProctoring() - App.jsx line 268\nconst stream = await navigator.mediaDevices.getUserMedia({\n  video: {\n    width: { ideal: 480 },   // Reduced from 640\n    height: { ideal: 360 },  // Reduced from 480\n    facingMode: 'user'\n  },\n  audio: false,\n});\n```\n\n**Impact**: 20-30% CPU reduction, slight accuracy loss\n\n### 2. **Batch Alert Processing**\n\n```javascript\nimport { OperationBatcher } from './advanced-utils.js';\n\nconst alertBatcher = new OperationBatcher(10, 100);\n\n// Instead of immediate addAlert():\nalertBatcher.add(() => addAlert(message, type, severity));\n```\n\n**Impact**: Reduces React re-renders\n\n### 3. **Throttle Canvas Rendering**\n\n```javascript\nimport { throttle } from './advanced-utils.js';\n\nconst throttledDraw = throttle(\n  () => drawOverlays(canvas, video, landmarks, headDir, suspObjects),\n  16  // ~60fps\n);\n```\n\n### 4. **Lazy Load Models**\n\n```javascript\n// Instead of loading all models at start:\nlet cocoModelLoaded = false;\n\n// Only load when object detection needed:\nif (!cocoModelLoaded && fc % FRAME_SKIP_OBJECT_DETECTION === 0) {\n  cocoRef.current = cocoRef.current || (await cocoSsd.load());\n  cocoModelLoaded = true;\n}\n```\n\n## Server-Side Optimization (Python)\n\n### GPU Acceleration\n\n```bash\n# NVIDIA GPU\npython -c \"import torch; print(torch.cuda.is_available())\"\n\n# Use GPU in code\nresults = model(frame, device='0')  # GPU 0\n```\n\n### Model Quantization\n\n```python\nfrom ultralytics import YOLO\n\n# Use INT8 quantized model (50% faster)\nmodel = YOLO('yolov8n-int8.pt')\n```\n\n## Deployment Checklist\n\n- [ ] Test on target hardware\n- [ ] Enable production minification\n- [ ] Disable debug logging\n- [ ] Set appropriate thresholds\n- [ ] Monitor memory usage\n- [ ] Test camera permissions\n- [ ] Verify HTTPS setup\n- [ ] Load test with multiple users\n\n## Monitoring in Production\n\n### Browser Console Logging\n\n```javascript\n// In config.js\nDEBUG: {\n  ENABLED: process.env.NODE_ENV === 'development',\n  LOG_FRAME_RATE: true,\n  LOG_DETECTION_RESULTS: false,\n}\n```\n\n### Analytics Integration\n\n```javascript\n// Send metrics to server\nconst reportMetrics = (fps, cpuUsage, memoryUsage) => {\n  fetch('/api/metrics', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ fps, cpuUsage, memoryUsage })\n  });\n};\n```\n\n## Troubleshooting Performance\n\n### Issue: Low FPS (<15)\n\n**Solutions** (in order):\n1. Increase `FRAME_SKIP_OBJECT_DETECTION` to 7-8\n2. Reduce video resolution to 480x360\n3. Disable COCO-SSD object detection\n4. Lower `minDetectionConfidence` to 0.3\n\n### Issue: High Memory (>500MB)\n\n**Solutions**:\n1. Reduce `MAX_ALERTS_HISTORY` to 100\n2. Clear browser cache\n3. Restart browser\n4. Enable memory profiling to find leaks\n\n### Issue: Many False Alarms\n\n**Solutions**:\n1. Increase detection thresholds by 2-3\n2. Increase cooldown periods\n3. Better lighting/camera positioning\n4. Increase `minTrackingConfidence` to 0.65\n\n## Performance Comparison\n\n**Configuration A** (Balanced):\n```javascript\nFRAME_SKIP_FACE = 3\nFRAME_SKIP_OBJECT = 5\nGAZE_THRESHOLD = 8\nDetection conf = 0.45\nResult: 20+ FPS, ~200MB, Good accuracy\n```\n\n**Configuration B** (High Accuracy):\n```javascript\nFRAME_SKIP_FACE = 2\nFRAME_SKIP_OBJECT = 3\nGAZE_THRESHOLD = 6\nDetection conf = 0.5\nResult: 12-15 FPS, ~250MB, Excellent accuracy\n```\n\n**Configuration C** (Low Resource):\n```javascript\nFRAME_SKIP_FACE = 4\nFRAME_SKIP_OBJECT = 8\nGAZE_THRESHOLD = 10\nDetection conf = 0.4\nResult: 30+ FPS, ~120MB, Good accuracy\n```\n\n## Additional Resources\n\n- [MediaPipe Performance](https://developers.google.com/mediapipe/solutions/guide)\n- [TensorFlow.js Optimization](https://js.tensorflow.org/guide/usage_and_performance)\n- [Web Performance APIs](https://developer.mozilla.org/en-US/docs/Web/API/Performance)\n- [Chrome DevTools Performance](https://developer.chrome.com/docs/devtools/performance/)\n"